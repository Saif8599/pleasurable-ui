<nav class="main-nav">
  <a href="/" class="logo">
    <img src="../assets/logo-bieb-in-bloei-wit.svg" alt="logo bieb in bloei">
  </a>

  <details id="hamburger-menu" class="nav-dropdown">
    <summary id="menu-toggle"></summary>
  </details>

  <ul class="nav-menu">
    <li><a href="/">Home</a></li>
    <li><a href="/stekjes">Stekjes</a></li>
    <li><a href="/zaden">Zaden</a></li>
    <li><a href="/geveltuin">Geveltuin</a></li>
    <li><a href="/agenda">Agenda</a></li>
    <li><a href="/partners">Partners</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
</nav>

<style>
nav {
  background-color: var(--color-primary);
  padding: 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

summary {
  position: relative;
  font-size: 2rem;
  color: var(--color-bg-main);
  z-index: 1001;
}

.nav-menu {
  position: fixed;
  inset: 0 0 auto 0;
  background: var(--color-primary);
  transform: translateY(-100%);
  transition: transform 0.3s ease;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 6rem 1rem 2rem;
  gap: 1rem;
  & a {
    color: var(--text-light);
  }
  & a:hover {
    color: var(--color-accent);
  }
}

#hamburger-menu:not([open]) summary::before {
  content: "☰";
}
#hamburger-menu[open] summary::before {
  content: "✕";
}

#hamburger-menu[open] + .nav-menu {
  transform: translateY(0);
}

@media (min-width: 768px) {
  .nav-dropdown {
    display: none;
  }

  .nav-menu {
    position: static;
    transform: none;
    flex-direction: row;
    justify-content: flex-end;
    padding: 0;
  }
}
</style>